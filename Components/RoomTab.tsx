import React from 'react';
import { Text } from 'react-native';
import AppContainer from './AppContainer';

export default function RoomTabComponent() {
    return (
        <AppContainer>
            <Text> Welcome in the room !
            </Text>
        </AppContainer>
    )
}